From: Shadow-MSM contributors
Subject: [PATCH] ARM: add K3765-Z resident-runtime early trace

This experimental out-of-tree patch provides observable zImage decompressor
and early kernel-entry milestones on the ZTE K3765-Z. It calls the initialized,
RAM-resident ARMPRG diagnostic string routine at physical address 0x00816cf4.
A dedicated trace stack lives at 0x008ff000 because Linux repurposes r13
before the ARM926 setup callback. A temporary non-cacheable identity map keeps
the resident runtime reachable across the first MMU transition. Additional
checkpoints bracket __mmap_switched, BSS clearing, the start_kernel branch,
early generic initialization, ARM device-tree setup, and the paging_init
boundary. The diagnostic build preserves the single 2 MiB identity-map entry
covering the resident runtime while Linux replaces the rest of the bootstrap
table. The device tree keeps the monitor's occupied 1 MiB window reserved.

This patch is intentionally device-specific and is not suitable for a generic
ARM kernel.

diff --git a/arch/arm/Kconfig.debug b/arch/arm/Kconfig.debug
index c345775..713af98 100644
--- a/arch/arm/Kconfig.debug
+++ b/arch/arm/Kconfig.debug
@@ -1918,6 +1918,15 @@ config DEBUG_UNCOMPRESS
 	  When this option is set, the selected DEBUG_LL output method
 	  will be re-used for normal decompressor output on multiplatform
 	  kernels.
+
+config SHADOW_MSM_EARLY_TRACE
+	bool "Shadow-MSM early trace through the resident ARMPRG runtime"
+	depends on CPU_ARM926T
+	help
+	  Emit RAM-only K3765-Z decompressor and pre-MMU kernel milestones
+	  through the initialized ARMPRG diagnostic transport at physical
+	  address 0x00816cf4. This is an experimental Shadow-MSM bring-up
+	  option and is not portable to other ARM926 systems.
 	  
 
 config UNCOMPRESS_INCLUDE
diff --git a/arch/arm/boot/compressed/misc.c b/arch/arm/boot/compressed/misc.c
index abfed1a..19d6598 100644
--- a/arch/arm/boot/compressed/misc.c
+++ b/arch/arm/boot/compressed/misc.c
@@ -91,6 +91,17 @@ static void putstr(const char *ptr)
 {
 	char c;
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	/*
+	 * Shadow-MSM enters the zImage while the initialized OEM ARMPRG
+	 * runtime is still resident at 0x00800000. Its bounded diagnostic
+	 * string transmitter provides an observable pre-MMU console without
+	 * touching persistent storage.
+	 */
+	((void (*)(const char *))0x00816cf4UL)(ptr);
+	return;
+#endif
+
 	while ((c = *ptr++) != '\0') {
 		if (c == '\n')
 			putc('\r');
diff --git a/arch/arm/kernel/head.S b/arch/arm/kernel/head.S
index 29e2900..c919cd3 100644
--- a/arch/arm/kernel/head.S
+++ b/arch/arm/kernel/head.S
@@ -104,12 +104,22 @@ ENTRY(stext)
 	@ ensure svc mode and all interrupts masked
 	safe_svcmode_maskall r9
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_entered
+	bl	__shadow_msm_trace
+#endif
+
 	mrc	p15, 0, r9, c0, c0		@ get processor id
 	bl	__lookup_processor_type		@ r5=procinfo r9=cpuid
 	movs	r10, r5				@ invalid processor (r5=0)?
  THUMB( it	eq )		@ force fixup-able long branch encoding
 	beq	__error_p			@ yes, error 'p'
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_cpu_ok
+	bl	__shadow_msm_trace
+#endif
+
 #ifdef CONFIG_ARM_LPAE
 	mrc	p15, 0, r3, c0, c1, 4		@ read ID_MMFR0
 	and	r3, r3, #0xf			@ extract VMSA support
@@ -138,6 +148,16 @@ ENTRY(stext)
 #endif
 	bl	__create_page_tables
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_tables_ok
+	bl	__shadow_msm_trace
+#endif
+
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_cpu_setup
+	bl	__shadow_msm_trace
+#endif
+
 	/*
 	 * The following calls CPU specific code in a position independent
 	 * manner.  See arch/arm/mm/proc-*.S for details.  r10 = base of
@@ -169,10 +189,53 @@ ENTRY(stext)
 	ldr	r12, [r10, #PROCINFO_INITFUNC]
 	add	r12, r12, r10
 	ret	r12
-1:	b	__enable_mmu
+1:
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_cpu_setup_returned
+	bl	__shadow_msm_trace
+#endif
+	b	__enable_mmu
 ENDPROC(stext)
 	.ltorg
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+/*
+ * Linux intentionally repurposes r13 as the future virtual
+ * __mmap_switched address before the processor setup callback. A trace after
+ * that point must not push to the apparent stack. Switch to a dedicated
+ * physical stack inside the monitor's reserved 0x00800000..0x008fffff
+ * window, preserve the Linux entry-contract registers and condition flags,
+ * then restore the original r13. r3 and r11 are scratch at every call site.
+ */
+ENTRY(__shadow_msm_trace)
+	mov	r3, sp
+	ldr	sp, =0x008ff000
+	push	{r0-r12, lr}
+	mrs	r12, cpsr
+	push	{r12, lr}
+	mov	r0, r11
+	ldr	r3, =0x00816cf4
+	blx	r3
+	pop	{r12, lr}
+	msr	cpsr_f, r12
+	pop	{r0-r12, lr}
+	mov	sp, r3
+	bx	lr
+ENDPROC(__shadow_msm_trace)
+
+.Lshadow_msm_entered:
+	.asciz	"Shadow-MSM: entered decompressed Linux head.S\r\n"
+.Lshadow_msm_cpu_ok:
+	.asciz	"Shadow-MSM: ARM926 processor lookup passed\r\n"
+.Lshadow_msm_tables_ok:
+	.asciz	"Shadow-MSM: initial page tables created\r\n"
+.Lshadow_msm_cpu_setup:
+	.asciz	"Shadow-MSM: calling ARM926 processor setup\r\n"
+.Lshadow_msm_cpu_setup_returned:
+	.asciz	"Shadow-MSM: ARM926 setup returned; enabling MMU next\r\n"
+	.align	2
+#endif
+
 /*
  * Setup the initial page tables.  We only setup the barest
  * amount which are required to get the kernel running, which
@@ -228,6 +228,18 @@ __create_page_tables:
 #endif
 #endif
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	ldr	r7, [r10, #PROCINFO_IO_MMUFLAGS]
+	mov	r0, r4
+	mov	r5, #0
+	mov	r6, #0x1000
+2:	orr	r3, r7, r5, lsl #SECTION_SHIFT
+	str	r3, [r0], #1 << PMD_ENTRY_ORDER
+	add	r5, r5, #1
+	subs	r6, r6, #1
+	bne	2b
+#endif
+
 	ldr	r7, [r10, #PROCINFO_MM_MMUFLAGS] @ mm_mmuflags
 
 	/*
@@ -503,11 +515,20 @@ ENTRY(__turn_mmu_on)
 	mcr	p15, 0, r0, c1, c0, 0		@ write control reg
 	mrc	p15, 0, r3, c0, c0, 0		@ read id reg
 	instr_sync
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_mmu_enabled
+	bl	__shadow_msm_trace
+#endif
 	mov	r3, r3
 	mov	r3, r13
 	ret	r3
 __turn_mmu_on_end:
 ENDPROC(__turn_mmu_on)
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+.Lshadow_msm_mmu_enabled:
+	.asciz	"Shadow-MSM: MMU enabled; identity execution continues\r\n"
+	.align	2
+#endif
 	.popsection
 
 
diff --git a/arch/arm/kernel/head-common.S b/arch/arm/kernel/head-common.S
--- a/arch/arm/kernel/head-common.S
+++ b/arch/arm/kernel/head-common.S
@@ -75,6 +75,10 @@
  */
 	__INIT
 __mmap_switched:
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_mmap_entered
+	bl	__shadow_msm_trace
+#endif
 
 	mov	r7, r1
 	mov	r8, r2
@@ -104,6 +108,10 @@ __mmap_switched:
 	sub	r2, r1, r0
 	mov	r1, #0
 	bl	__memset			@ clear .bss
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_bss_cleared
+	bl	__shadow_msm_trace
+#endif
 
 	adr_l	r0, init_task			@ get swapper task_struct
 	set_current r0, r1
@@ -117,9 +125,23 @@ __mmap_switched:
 #ifdef CONFIG_KASAN
 	bl	kasan_early_init
 #endif
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_start_kernel
+	bl	__shadow_msm_trace
+#endif
 	mov	lr, #0
 	b	start_kernel
 ENDPROC(__mmap_switched)
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+.Lshadow_msm_mmap_entered:
+	.asciz	"Shadow-MSM: entered __mmap_switched at the kernel virtual address\r\n"
+.Lshadow_msm_bss_cleared:
+	.asciz	"Shadow-MSM: __mmap_switched cleared BSS\r\n"
+.Lshadow_msm_start_kernel:
+	.asciz	"Shadow-MSM: branching to start_kernel\r\n"
+	.align	2
+#endif
+
 	.align	2
 	.type	__mmap_switched_data, %object
 __mmap_switched_data:
diff --git a/init/main.c b/init/main.c
index aa21add..20a7274 100644
--- a/init/main.c
+++ b/init/main.c
@@ -933,17 +933,36 @@ static void __init print_unknown_bootoptions(void)
 	memblock_free(unknown_options, len);
 }
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+static __always_inline void shadow_msm_c_trace(const char *message)
+{
+	((void (*)(const char *))0x00816cf4UL)(message);
+}
+#endif
+
 asmlinkage __visible void __init __no_sanitize_address start_kernel(void)
 {
 	char *command_line;
 	char *after_dashes;
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: entered start_kernel C code\r\n");
+#endif
 	set_task_stack_end_magic(&init_task);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: initial task stack is ready\r\n");
+#endif
 	smp_setup_processor_id();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: processor ID setup completed\r\n");
+#endif
 	debug_objects_early_init();
 	init_vmlinux_build_id();
 
 	cgroup_init_early();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: earliest generic initialization completed\r\n");
+#endif
 
 	local_irq_disable();
 	early_boot_irqs_disabled = true;
@@ -956,21 +975,60 @@ asmlinkage __visible void __init __no_sanitize_address start_kernel(void)
 	page_address_init();
 	pr_notice("%s", linux_banner);
 	early_security_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: entering ARM setup_arch\r\n");
+#endif
 	setup_arch(&command_line);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: setup_arch returned to start_kernel\r\n");
+#endif
 	setup_boot_config();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: boot configuration completed\r\n");
+#endif
 	setup_command_line(command_line);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: command line storage completed\r\n");
+#endif
 	setup_nr_cpu_ids();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: CPU ID limits completed\r\n");
+#endif
 	setup_per_cpu_areas();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: per-CPU areas completed\r\n");
+#endif
 	smp_prepare_boot_cpu();	/* arch-specific boot-cpu hooks */
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: boot CPU preparation completed\r\n");
+#endif
 	boot_cpu_hotplug_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: boot CPU hotplug state completed\r\n");
+#endif
 
 	build_all_zonelists(NULL);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: initial zone lists completed\r\n");
+#endif
 	page_alloc_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: page allocator initialization completed\r\n");
+#endif
 
 	pr_notice("Kernel command line: %s\n", saved_command_line);
 	/* parameters may set static keys */
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: entering jump-label initialization\r\n");
+#endif
 	jump_label_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: jump-label initialization completed\r\n");
+#endif
 	parse_early_param();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: early parameters parsed\r\n");
+#endif
 	after_dashes = parse_args("Booting kernel",
 				  static_command_line, __start___param,
 				  __stop___param - __start___param,
@@ -982,24 +1040,51 @@ asmlinkage __visible void __init __no_sanitize_address start_kernel(void)
 	if (extra_init_args)
 		parse_args("Setting extra init args", extra_init_args,
 			   NULL, 0, -1, -1, NULL, set_init_arg);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: kernel arguments parsed\r\n");
+#endif
 
 	/* Architectural and non-timekeeping rng init, before allocator init */
 	random_init_early(command_line);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: early random initialization completed\r\n");
+#endif
 
 	/*
 	 * These use large bootmem allocations and must precede
 	 * kmem_cache_init()
 	 */
 	setup_log_buf(0);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: kernel log buffer completed\r\n");
+#endif
 	vfs_caches_init_early();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: early VFS caches completed\r\n");
+#endif
 	sort_main_extable();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: exception table sorting completed\r\n");
+#endif
 	trap_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: generic trap initialization completed\r\n");
+#endif
 	mm_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: generic memory management completed\r\n");
+#endif
 
 	ftrace_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: function tracing initialization completed\r\n");
+#endif
 
 	/* trace_printk can be enabled here */
 	early_trace_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: early trace infrastructure completed\r\n");
+#endif
 
 	/*
 	 * Set up the scheduler prior starting any interrupts (such as the
@@ -1007,18 +1092,30 @@ asmlinkage __visible void __init __no_sanitize_address start_kernel(void)
 	 * time - but meanwhile we still have a functioning scheduler.
 	 */
 	sched_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: scheduler initialization completed\r\n");
+#endif
 
 	if (WARN(!irqs_disabled(),
 		 "Interrupts were enabled *very* early, fixing it\n"))
 		local_irq_disable();
 	radix_tree_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: radix tree initialization completed\r\n");
+#endif
 	maple_tree_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: maple tree initialization completed\r\n");
+#endif
 
 	/*
 	 * Set up housekeeping before setting up workqueues to allow the unbound
 	 * workqueue to take non-housekeeping into account.
 	 */
 	housekeeping_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: housekeeping initialization completed\r\n");
+#endif
 
 	/*
 	 * Allow workqueue creation and work item queueing/cancelling
@@ -1026,51 +1123,128 @@ asmlinkage __visible void __init __no_sanitize_address start_kernel(void)
 	 * workqueue_init().
 	 */
 	workqueue_init_early();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: early workqueue initialization completed\r\n");
+#endif
 
 	rcu_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: RCU initialization completed\r\n");
+#endif
 
 	/* Trace events are available after this */
 	trace_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: trace event initialization completed\r\n");
+#endif
 
 	if (initcall_debug)
 		initcall_debug_enable();
 
 	context_tracking_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: context tracking initialization completed\r\n");
+#endif
 	/* init some links before init_ISA_irqs() */
 	early_irq_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: early IRQ initialization completed\r\n");
+	shadow_msm_c_trace("Shadow-MSM: entering platform IRQ initialization\r\n");
+#endif
 	init_IRQ();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: platform IRQ initialization completed\r\n");
+#endif
 	tick_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: tick initialization completed\r\n");
+#endif
 	rcu_init_nohz();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: no-HZ RCU initialization completed\r\n");
+#endif
 	init_timers();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: timer core initialization completed\r\n");
+#endif
 	srcu_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: SRCU initialization completed\r\n");
+#endif
 	hrtimers_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: high-resolution timer initialization completed\r\n");
+#endif
 	softirq_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: softirq initialization completed\r\n");
+#endif
 	timekeeping_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: timekeeping initialization completed\r\n");
+	shadow_msm_c_trace("Shadow-MSM: entering platform time initialization\r\n");
+#endif
 	time_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: platform time initialization completed\r\n");
+#endif
 
 	/* This must be after timekeeping is initialized */
 	random_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: random subsystem initialization completed\r\n");
+#endif
 
 	/* These make use of the fully initialized rng */
 	kfence_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: KFENCE initialization completed\r\n");
+#endif
 	boot_init_stack_canary();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: boot stack canary initialized\r\n");
+#endif
 
 	perf_event_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: performance events initialized\r\n");
+#endif
 	profile_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: profiling initialized\r\n");
+#endif
 	call_function_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: cross-CPU call infrastructure initialized\r\n");
+#endif
 	WARN(!irqs_disabled(), "Interrupts were enabled early\n");
 
 	early_boot_irqs_disabled = false;
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: enabling CPU interrupts\r\n");
+#endif
 	local_irq_enable();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: CPU interrupts enabled\r\n");
+#endif
 
 	kmem_cache_init_late();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: late slab initialization completed\r\n");
+#endif
 
 	/*
 	 * HACK ALERT! This is early. We're enabling the console before
 	 * we've done PCI setups etc, and console_init() must be aware of
 	 * this. But we do want output early, in case something goes wrong.
 	 */
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: entering console initialization\r\n");
+#endif
 	console_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_c_trace("Shadow-MSM: console initialization completed\r\n");
+#endif
 	if (panic_later)
 		panic("Too many boot %s vars at `%s'", panic_later,
 		      panic_param);
diff --git a/arch/arm/kernel/setup.c b/arch/arm/kernel/setup.c
index cb88c6e..8b58de7 100644
--- a/arch/arm/kernel/setup.c
+++ b/arch/arm/kernel/setup.c
@@ -83,6 +83,13 @@ extern void adjust_lowmem_bounds(void);
 extern enum reboot_mode reboot_mode;
 extern void setup_dma_zone(const struct machine_desc *desc);
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+static __always_inline void shadow_msm_arch_trace(const char *message)
+{
+	((void (*)(const char *))0x00816cf4UL)(message);
+}
+#endif
+
 unsigned int processor_id;
 EXPORT_SYMBOL(processor_id);
 unsigned int __machine_arch_type __read_mostly;
@@ -1097,10 +1104,16 @@ void __init setup_arch(char **cmdline_p)
 	const struct machine_desc *mdesc = NULL;
 	void *atags_vaddr = NULL;
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: entered setup_arch\r\n");
+#endif
 	if (__atags_pointer)
 		atags_vaddr = FDT_VIRT_BASE(__atags_pointer);
 
 	setup_processor();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: setup_processor completed\r\n");
+#endif
 	if (atags_vaddr) {
 		mdesc = setup_machine_fdt(atags_vaddr);
 		if (mdesc)
@@ -1121,6 +1134,9 @@ void __init setup_arch(char **cmdline_p)
 	machine_desc = mdesc;
 	machine_name = mdesc->name;
 	dump_stack_set_arch_desc("%s", mdesc->name);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: device-tree machine selected\r\n");
+#endif
 
 	if (mdesc->reboot_mode != REBOOT_HARD)
 		reboot_mode = mdesc->reboot_mode;
@@ -1138,6 +1154,9 @@ void __init setup_arch(char **cmdline_p)
 
 #ifdef CONFIG_MMU
 	early_mm_init(mdesc);
+#endif
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: early_mm_init completed\r\n");
 #endif
 	setup_dma_zone(mdesc);
 	xen_early_init();
@@ -1150,9 +1169,18 @@ void __init setup_arch(char **cmdline_p)
 	arm_memblock_init(mdesc);
 	/* Memory may have been removed so recalculate the bounds. */
 	adjust_lowmem_bounds();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: ARM memblock initialization completed\r\n");
+#endif
 
 	early_ioremap_reset();
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: entering paging_init\r\n");
+#endif
 	paging_init(mdesc);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_arch_trace("Shadow-MSM: paging_init completed\r\n");
+#endif
 	kasan_init();
 	request_standard_resources(mdesc);
diff --git a/arch/arm/mm/mmu.c b/arch/arm/mm/mmu.c
--- a/arch/arm/mm/mmu.c
+++ b/arch/arm/mm/mmu.c
@@ -1311,8 +1311,13 @@ static __init void prepare_page_table(void)
 	for (addr = KASAN_SHADOW_END; addr < MODULES_VADDR; addr += PMD_SIZE)
 		pmd_clear(pmd_off_k(addr));
 #else
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	/* Preserve the bootstrap identity map used by the resident USB monitor. */
+	addr = MODULES_VADDR;
+#else
 	for (addr = 0; addr < MODULES_VADDR; addr += PMD_SIZE)
 		pmd_clear(pmd_off_k(addr));
+#endif
 #endif
 
 #ifdef CONFIG_XIP_KERNEL
@@ -1366,6 +1372,13 @@ void __init arm_mm_memblock_reserve(void)
 #endif
 }
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+static __always_inline void shadow_msm_mmu_trace(const char *message)
+{
+	((void (*)(const char *))0x00816cf4UL)(message);
+}
+#endif
+
 /*
  * Set up the device mappings.  Since we clear out the page tables for all
  * mappings above VMALLOC_START, except early fixmap, we might remove debug
@@ -1383,14 +1396,23 @@ static void __init devicemaps_init(const struct machine_desc *mdesc)
 	 * Allocate the vector page early.
 	 */
 	vectors = early_alloc(PAGE_SIZE * 2);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: vector pages allocated\r\n");
+#endif
 
 	early_trap_init(vectors);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: early trap vectors initialized\r\n");
+#endif
 
 	/*
 	 * Clear page table except top pmd used by early fixmaps
 	 */
 	for (addr = VMALLOC_START; addr < (FIXADDR_TOP & PMD_MASK); addr += PMD_SIZE)
 		pmd_clear(pmd_off_k(addr));
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: temporary vmalloc mappings cleared\r\n");
+#endif
 
 	if (__atags_pointer) {
 		/* create a read-only mapping of the device tree */
@@ -1468,9 +1487,15 @@ static void __init devicemaps_init(const struct machine_desc *mdesc)
 	else
 		debug_ll_io_init();
 	fill_pmd_gaps();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: static device mappings completed\r\n");
+#endif
 
 	/* Reserve fixed i/o space in VMALLOC region */
 	pci_reserve_io();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: PCI I/O reservation completed\r\n");
+#endif
 
 	/*
 	 * Finally flush the caches and tlb to ensure that we're in a
@@ -1479,10 +1504,19 @@ static void __init devicemaps_init(const struct machine_desc *mdesc)
 	 * back.  After this point, we can start to touch devices again.
 	 */
 	local_flush_tlb_all();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: device-map TLB flush completed\r\n");
+#endif
 	flush_cache_all();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: device-map cache flush completed\r\n");
+#endif
 
 	/* Enable asynchronous aborts */
 	early_abt_enable();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: asynchronous aborts enabled\r\n");
+#endif
 }
 
 static void __init kmap_init(void)
@@ -1766,7 +1800,13 @@ void __init paging_init(const struct machine_desc *mdesc)
 		 kernel_sec_start, kernel_sec_end);
 
 	prepare_page_table();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: low bootstrap identity mappings retained\r\n");
+#endif
 	map_lowmem();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: map_lowmem completed\r\n");
+#endif
 	memblock_set_current_limit(arm_lowmem_limit);
 	pr_debug("lowmem limit is %08llx\n", (long long)arm_lowmem_limit);
 	/*
@@ -1774,9 +1814,27 @@ void __init paging_init(const struct machine_desc *mdesc)
 	 * be used
 	 */
 	map_kernel();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: permanent kernel mappings completed\r\n");
+#endif
 	dma_contiguous_remap();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: DMA contiguous remap completed\r\n");
+#endif
 	early_fixmap_shutdown();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: early fixmap shutdown completed\r\n");
+#endif
 	devicemaps_init(mdesc);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: ARM device mappings completed\r\n");
+#endif
 	kmap_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: permanent kmap tables completed\r\n");
+#endif
 	tcm_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: TCM initialization completed\r\n");
+#endif
 
@@ -1786,9 +1844,18 @@ void __init paging_init(const struct machine_desc *mdesc)
 	zero_page = early_alloc(PAGE_SIZE);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: zero page allocated\r\n");
+#endif
 
 	bootmem_init();
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: bootmem_init completed\r\n");
+#endif
 
 	empty_zero_page = virt_to_page(zero_page);
 	__flush_dcache_page(NULL, empty_zero_page);
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	shadow_msm_mmu_trace("Shadow-MSM: leaving paging_init\r\n");
+#endif
 }
 
 void __init early_mm_init(const struct machine_desc *mdesc)
diff --git a/arch/arm/mm/proc-arm926.S b/arch/arm/mm/proc-arm926.S
index 134426e..b5aaf97 100644
--- a/arch/arm/mm/proc-arm926.S
+++ b/arch/arm/mm/proc-arm926.S
@@ -455,12 +455,25 @@ ENDPROC(cpu_arm926_do_resume)
 
 	.type	__arm926_setup, #function
 __arm926_setup:
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	mov	r7, lr
+	adr_l	r11, .Lshadow_msm_arm926_entered
+	bl	__shadow_msm_trace
+#endif
 	mov	r0, #0
 	mcr	p15, 0, r0, c7, c7		@ invalidate I,D caches on v4
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_arm926_cache_ok
+	bl	__shadow_msm_trace
+#endif
 	mcr	p15, 0, r0, c7, c10, 4		@ drain write buffer on v4
 #ifdef CONFIG_MMU
 	mcr	p15, 0, r0, c8, c7		@ invalidate I,D TLBs on v4
 #endif
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_arm926_tlb_ok
+	bl	__shadow_msm_trace
+#endif
 
 
 #ifdef CONFIG_CPU_DCACHE_WRITETHROUGH
@@ -478,9 +491,26 @@ __arm926_setup:
 #ifdef CONFIG_CPU_CACHE_ROUND_ROBIN
 	orr	r0, r0, #0x4000			@ .1.. .... .... ....
 #endif
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+	adr_l	r11, .Lshadow_msm_arm926_control_ok
+	bl	__shadow_msm_trace
+	mov	lr, r7
+#endif
 	ret	lr
 	.size	__arm926_setup, . - __arm926_setup
 
+#ifdef CONFIG_SHADOW_MSM_EARLY_TRACE
+.Lshadow_msm_arm926_entered:
+	.asciz	"Shadow-MSM: entered ARM926 setup\r\n"
+.Lshadow_msm_arm926_cache_ok:
+	.asciz	"Shadow-MSM: ARM926 cache invalidate returned\r\n"
+.Lshadow_msm_arm926_tlb_ok:
+	.asciz	"Shadow-MSM: ARM926 TLB invalidate returned\r\n"
+.Lshadow_msm_arm926_control_ok:
+	.asciz	"Shadow-MSM: ARM926 control word ready\r\n"
+	.align	2
+#endif
+
 	/*
 	 *  R
 	 * .RVI ZFRS BLDP WCAM
